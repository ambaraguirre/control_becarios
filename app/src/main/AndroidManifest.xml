<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Ordinario"
        tools:targetApi="31">
        <activity
            android:name=".verinfo2"
            android:exported="false"
            android:parentActivityName=".pantallaAdministrador"
            />
        <activity
            android:name=".verinfo"
            android:exported="false"
            android:parentActivityName=".pantallaUsuario" />
        <activity
            android:name=".agregarBecario"
            android:exported="false"
            android:parentActivityName=".pantallaUsuario" />
        <activity
            android:name=".pantallaUsuario"
            android:exported="false" />
        <activity
            android:name=".agregarUsuario"
            android:exported="false"
            android:parentActivityName=".pantallaAdministrador" />
        <activity
            android:name=".pantallaAdministrador"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>